// export const setToken = (token, ctx = null) => {
//   if (!_.isNil(ctx)) {
//     nookies.set(ctx, 'same_page', token, { path: '/' });
//   } else {
//     Cookie.set('same_page', token);
//   }
// };
